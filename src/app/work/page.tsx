import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Work | Maynkudu",
};

const WorkPage = () => {
    return (
        <div>
            <div>WorkPage</div>
        </div>
    );
};

export default WorkPage;
